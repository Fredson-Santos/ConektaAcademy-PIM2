# Como Executar o Sistema

## 🖥️ Modo Interface Web (Streamlit)

### Windows:
```bash
run.bat
```
ou
```bash
streamlit run interface/app.py
```

### Linux/Mac:
```bash
chmod +x run.sh
./run.sh
```
ou
```bash
streamlit run interface/app.py
```

## 💻 Modo Terminal (CLI)

```bash
python terminal/main.py
```

## ⚠️ Importante

- O arquivo `app.py` agora está em `interface/app.py`
- O arquivo principal do terminal está em `terminal/main.py`
- Use os scripts `run.bat` (Windows) ou `run.sh` (Linux/Mac) para facilitar a execução

